<%# 部分テンプレート化する%>
<%# 同じフォルダー内にある_error.html.erbを持ってきて、recipeを@recipeとして使う%>
<%= render 'error', recipe: @recipe %>

<div class="container mt-5">
  <div class="row">
    <div class="col-6 mx-auto">
        <h2>新規投稿</h2>
        <%# 部分テンプレート化する%>
        <%# 同じフォルダー内にある_form.html.erbを持ってきて、recipeを@recipeとして使う%>
        <%= render 'form', recipe: @recipe %>

    </div>
  </div>
</div>